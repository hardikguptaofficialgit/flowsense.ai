// Firebase is no longer used in the frontend
// All authentication is handled by the backend session auth system
// Backend can optionally use Firebase for server-side analytics
// See backend/.env for Firebase configuration if needed

export const auth = null;
export const db = null;
export const googleProvider = null;
export const hasFirebaseConfig = false;
export const analytics = null;
